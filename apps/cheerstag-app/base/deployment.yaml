apiVersion: apps/v1
kind: Deployment
metadata:
    name: cheerstag-app
    namespace: cheerstag-app
    labels:
        app: cheerstag-app
spec:
    replicas: 1
    selector:
        matchLabels:
            app: cheerstag-app
    template:
        metadata:
            labels:
                app: cheerstag-app
        spec:
            containers:
                - name: web
                  image: ghcr.io/axiom-o-choice/cheerstag-app:3ff5f727487ccc456459b2b31e9e2b16fdd32a87
                  imagePullPolicy: Always
                  ports:
                      - containerPort: 80
                  readinessProbe:
                      httpGet: { path: "/", port: 80 }
                      initialDelaySeconds: 3
                      periodSeconds: 10
                  livenessProbe:
                      httpGet: { path: "/", port: 80 }
                      initialDelaySeconds: 10
                      periodSeconds: 20
