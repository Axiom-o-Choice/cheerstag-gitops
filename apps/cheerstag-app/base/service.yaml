apiVersion: v1
kind: Service
metadata:
    name: slot-app
    namespace: slot-app
spec:
    selector:
        app: slot-app
    ports:
        - name: http
          port: 80
          targetPort: 80
    type: ClusterIP
